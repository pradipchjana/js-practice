const board = [
  ["A", "B", "B", "D", "E"],
  ["A", "C", "D", "A", "B"],
  ["B", "C", "D", "E", "S"]
]

const findSequence = sequence => {
  return true;
} 

const main = () => {
  const sequence = "ACCDESB"
  console.log(findSequence(sequence));
}